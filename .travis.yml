language: java
jdk: openjdk11

script:
  - ./gradlew clean
  - ./gradlew bootWar
  - ./gradlew build --info
  - ./gradlew dependencyUpdates
  - ./gradlew dependencyCheckAnalyze

cache:
  directories:
    - /home/travis/.gradle
